import 'package:flutter/material.dart';

class HalamanPolos extends StatelessWidget {
  const HalamanPolos({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Home"),
      ),
      body: Center(child: Text("Content Here")),
    );
  }
}
